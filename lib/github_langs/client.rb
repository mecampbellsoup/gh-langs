module GithubLangs
  class Client
    def initialize(credentials)
      @username = credentials.fetch(:username)
      @password = credentials.fetch(:password)
    end

    def user(

    end
  end
end
